import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: 50,
    alignItems: 'center',
    position: 'absolute',
    left: 20,
    top: 50,
    flexDirection: 'row',
  },
});
export default styles;
